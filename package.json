{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.173.0"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "vite": "^6.1.0"
  }
}
